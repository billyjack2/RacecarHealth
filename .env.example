# Root-level pointer. Each app/package has its own .env.example with the
# variables it actually consumes. Copy apps/web/.env.example to
# apps/web/.env.local for local dev.
#
# See:
#   apps/web/.env.example
#   packages/db/.env.example
