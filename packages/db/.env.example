# Only used by drizzle-kit (migrations). Uses the UNPOOLED URL because
# pgbouncer's transaction-pooling mode is incompatible with the DDL /
# advisory-lock patterns Drizzle migrations use.
POSTGRES_URL_NON_POOLING=postgres://user:pass@host/db?sslmode=require
